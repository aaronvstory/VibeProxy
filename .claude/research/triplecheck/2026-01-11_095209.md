
---

## Fix Log

### HIGH Issues Fixed (3)
1. **Live API Polling in UI Render Loop** - Added 30-second cache for model list to prevent UI blocking
2. **ConvertFrom-Json missing -Raw** - Added `-Raw -ErrorAction Stop` to all JSON reads in Switch-A0Config
3. **A0 settings JSON parse not protected** - Wrapped in try/catch with graceful error messages

### MEDIUM Issues Fixed (5)
4. **Write-Toast sleep duration** - Reduced from 2000ms to 1000ms for snappier UX
5. **Inefficient Add-Member in loop** - Changed to direct property assignment where property exists
6. **Potential JSON corruption** - Added null check before saving settings
7. **Set-Content encoding** - Added `-Encoding UTF8` to all JSON writes
8. **api_keys hashtable handling** - Now properly handles as PSCustomObject with conditional Add-Member

### Summary
- Script parses without errors ✓
- All HIGH + MEDIUM issues addressed ✓
- LOW issues remain (non-critical)
